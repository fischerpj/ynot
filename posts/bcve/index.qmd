---
title: BCVE
author: 'granPiero'
date: '2025-09-30'
image: "hurdler.jpg"
params:
  refs: Gen1:1
  refs_after: Rom1:1
  version: SG21
format: 
  html:
    comments:
      utterances: 
        repo: "fischerpj/ynot"
execute:
  echo: false
---

test of BCVE only for reaction to not recognized input.

if book is not a recognized abbreviation then osis: null, bbc: undefined and is_valid: false

<!-- inputDiv is considered Void-->

```{ojs}
import { Inputs } from "@observablehq/inputs";
import { html } from "@observablehq/stdlib";
import { Bcve, Ref } from "./BibleBooksCodes_Tables.js";

```

```{ojs}
//================== 1. INPUTS
// Editable input field
viewof RefInput = {
  const inputText = Inputs.text({
  value: "Gen1:1",
  style: "width: 80px;"
  })
  return inputText
}

```

```{ojs}
//================== REACTIVE PREVIEW OF INPUT 
RefPreview = {
  const mb = new Bcve(RefInput);
  return mb;
}

MiRef = {
  const mb = RefPreview;
  
////  if (mb.is_valid) {
    const mir = new Ref(mb.param);
//    await mir.fetch_parallel();   // Wait for data to be fetched
//    await mir.displayData(); 
//    return mir.outputContent;
//    return await mir.data;
      return mir;
///  } else {
///    return "undefined"
///  }
}  

RefPreview;
MiRef;

```

::: {#outputDiv}
<!--  any output comes here to outputDiv is considered Void-->
:::
